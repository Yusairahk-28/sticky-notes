from django.apps import AppConfig


# Define and set the class for Sticky Notes.
class StickyNotesAppConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'sticky_notes_app'
